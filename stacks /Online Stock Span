class StockSpanner {
    stack<pair< int , int >>  st; 
    int index = 0 ; 

public:
    StockSpanner() {

        
    }
    
    int next(int price) {

        int ans  ; 

        while(!st.empty() && st.top().first <= price)
        {
            st.pop() ;
        }

        if(st.empty())
        {
            ans = index + 1 ;
        }

        else
        {
            ans = index - st.top().second ;
        }

        st.push({price , index}) ; 

        index ++ ; 

        return ans ; 
  
    }
};

 */
